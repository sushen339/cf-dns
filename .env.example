# Cloudflare API token with permissions to manage zones and DNS records
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token

# Optional: customize the Express server port (defaults to 4000)
# PORT=4000

# Optional: specify allowed client origins for CORS (comma separated)
# CLIENT_ORIGIN=http://localhost:5173
